# Cloud Functions(gen2) 런타임 환경변수. 실제 값은 functions/.env 로 복사(커밋 금지 권장).
# 채점 백엔드: 아래가 설정되면 Cloud Run 러너(1차) → 실패 시 Wandbox(폴백).
# 비워두면 Wandbox 만 사용(무설정 기본 동작).

# cloud-run/deploy.sh 실행 후 출력되는 값을 넣는다.
JUDGE_URL=
JUDGE_SHARED_SECRET=
